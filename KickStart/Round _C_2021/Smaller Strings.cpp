#include <bits/stdc++.h>
using namespace std;
void solve(int n, int k, string S)
{
    int rlt = 0;

    cout << n << " " << k << " " << S;
}

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, k;
        string S;
        cin >> n >> k;
        getline(cin >> ws, S);
        solve(n, k, S);
    }
}